<!DOCTYPE html>
<html>
  <head>
    <style>
      * {
        font-family: sans-serif;
      }

      body {
        margin: 0;
        padding: 0 30px;
      }

      body p {
        text-align: justify;
      }

      body h1 {
        margin: .4em 0;
      }
    </style>
  </head>
  <body>
    <h1><%= title %></h1>

    <hr>
    <% textsArray.forEach(text => { %>
      <p>
        <%= text %>
      </p>
    <% }) %>
  </body>
</html>